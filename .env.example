# Environment consumed by `supabase/config.toml` when syncing hosted Auth settings.
# Keep real OAuth credentials in your shell, local password manager, or deployment secret store.
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=your_google_oauth_client_id
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret
