# 考勤底稿预生成与前日结算进度管控

## 1. 文档目标

本文档用于跟踪 `考勤底稿预生成与前日结算-任务列表.md` 中各任务的执行状态、实际产出、验收结果和遗留问题。

后续执行规则：

- 每完成一个任务，必须回填本文档。
- 每个任务只能在满足验收标准后标记为“已完成”。
- 如果任务只完成部分内容，应标记为“进行中”或“部分完成”，不得直接标记完成。
- 如果发现任务范围变化，应同步更新任务列表文档。
- 如果出现阻塞，应记录具体阻塞原因和下一步可执行动作。

关联文档：

- `docs/admin文档/考勤计算管理模块/考勤底稿预生成与前日结算设计文档.md`
- `docs/admin文档/考勤计算管理模块/考勤底稿预生成与前日结算-任务列表.md`

---

## 2. 状态说明

- 未开始：尚未执行。
- 进行中：已开始但未满足全部验收标准。
- 部分完成：已有可确认产出，但仍缺少关键验收项。
- 待确认：AI 已完成执行或草案，需要人工确认。
- 已完成：已满足验收标准，并已记录实际输出与验证结果。
- 阻塞：当前无法继续，需要先解决明确阻塞。

---

## 3. 总体进度

- 当前阶段：已完成实现与验证。
- 当前推荐下一步：由用户验收；如要上线，先应用 Supabase 迁移并按正式部署流程发布。
- 总任务数：13。
- 已完成：13。
- 进行中：0。
- 未开始：0。
- 阻塞：0。
- 本次回填时间：2026-06-05 21:56:44 PDT。

---

## 4. 任务进度明细

### 任务 1：冻结本轮业务范围与关键规则

- 状态：已完成。
- 执行方式：HITL。
- 负责人：用户确认，AI 记录。
- 开始时间：已在对话中完成。
- 完成时间：已在对话中完成。
- 实际完成内容：
  - 确认本轮只做考勤底稿预生成与前日结算。
  - 确认当天底稿初始状态使用 `pending`。
  - 确认业务时区为泰国时间。
  - 确认允许新增或调整数据库字段。
  - 确认当天列表需要展示 `pending` 底稿。
  - 确认优先级为：人工调整 > 休假 / 病假 > 打卡记录 > 自动缺勤。
  - 确认薪资联动已经做过，本轮只核对是否需要适配新流程。
- 实际输出物：
  - `考勤底稿预生成与前日结算-任务列表.md`
  - `考勤底稿预生成与前日结算-进度管控.md`
- 验证结果：关键业务规则已由用户确认。
- 遗留问题：无。
- 下一步：已进入实现并完成。

### 任务 2：梳理现有考勤相关数据模型

- 状态：已完成。
- 执行方式：AFK。
- 负责人：AI。
- 开始时间：2026-06-05 本轮实施开始。
- 完成时间：2026-06-05 21:56:44 PDT。
- 实际完成内容：
  - 确认 `attendance_records` 继续作为原始打卡 / 后台补卡记录表，不承载底稿状态。
  - 确认 `attendance_calculation_results.status` 作为 `pending` / `checked_in` / `absent` / `manual_adjusted` 等计算状态存放位置。
  - 新增 `attendance_calculation_results.calculation_phase`、`generated_by`、`settled_at`，用于区分底稿、结算来源和结算时间。
  - 保留 `attendance_record_id`、`source`、`note` 等字段作为人工调整和员工端打卡的追踪依据。
  - 确认 `monthly_attendance_summaries` 继续作为薪资前置汇总表，不直接存过程态底稿。
- 实际输出物：
  - `supabase/migrations/20260606070000_add_attendance_draft_statuses.sql`
  - `apps/admin/server/attendance-v2.js`
  - `apps/admin/server/index.js`
- 验证结果：
  - `node --check apps/admin/server/index.js` 通过。
  - `node --check apps/admin/server/attendance-v2.js` 通过。
- 遗留问题：数据库迁移需要在目标 Supabase 环境应用后才会真正落库。
- 下一步：已完成。

### 任务 3：定义考勤状态与流转规则

- 状态：已完成。
- 执行方式：AFK。
- 负责人：AI。
- 开始时间：2026-06-05 本轮实施开始。
- 完成时间：2026-06-05 21:56:44 PDT。
- 实际完成内容：
  - 定义当天无打卡底稿为 `pending` + `calculation_phase = draft`。
  - 定义当天已上班未下班为 `checked_in` + `calculation_phase = draft`。
  - 定义正式结算时无记录为 `absent`。
  - 定义正式结算时缺上班 / 缺下班为 `exception`。
  - 保留后台人工记录来源 `manual` 对应 `manual_adjusted`。
  - 保留 `leave` / `sick_leave` 优先级，不计工时和费用。
- 实际输出物：
  - `apps/admin/server/attendance-v2.js`
  - `apps/admin/server/attendance-v2.test.mjs`
  - `apps/admin/src/types.ts`
- 验证结果：`node apps/admin/server/attendance-v2.test.mjs` 通过。
- 遗留问题：无。
- 下一步：已完成。

### 任务 4：设计并实现底稿预生成服务

- 状态：已完成。
- 执行方式：AFK。
- 负责人：AI。
- 开始时间：2026-06-05 本轮实施开始。
- 完成时间：2026-06-05 21:56:44 PDT。
- 实际完成内容：
  - 新增 `fetchAttendanceTargetEmployees`，统一筛选应参与考勤的员工。
  - 新增 `generateAttendanceDraftsForDate(date, ownerUserId)`。
  - 底稿生成复用 `recalculateDailyAttendance`，保持单一计算口径。
  - 通过 upsert 复用原有 `attendance_calculation_results` 写入逻辑，重复执行不会新增重复行。
  - 当天无记录写为 `pending`，当天缺下班写为 `checked_in`。
- 实际输出物：
  - `apps/admin/server/index.js`
  - `apps/admin/server/attendance-v2.js`
- 验证结果：
  - `node apps/admin/server/attendance-v2.test.mjs` 通过。
  - `npm --workspace @wmshr/admin run lint` 通过。
- 遗留问题：需要上线环境应用新迁移后才能写入新增元数据字段；服务端保留旧 schema 降级写入，避免未迁移环境直接中断核心打卡/重算。
- 下一步：已完成。

### 任务 5：设计并实现前日结算服务

- 状态：已完成。
- 执行方式：AFK。
- 负责人：AI。
- 开始时间：2026-06-05 本轮实施开始。
- 完成时间：2026-06-05 21:56:44 PDT。
- 实际完成内容：
  - 新增 `settleAttendanceForDate(date, ownerUserId)`。
  - 正式结算不传 `pendingIfNoRecord` / `inProgressIfMissingOut`，因此历史无记录落为 `absent`，缺下班落为 `exception`。
  - 结算后刷新对应月份 `monthly_attendance_summaries`。
  - 新增 `runDailyAttendanceMaintenance`，固定顺序为先结算前一天，再生成当天底稿。
- 实际输出物：
  - `apps/admin/server/index.js`
- 验证结果：
  - `node --check apps/admin/server/index.js` 通过。
  - `npm --workspace @wmshr/admin run lint` 通过。
- 遗留问题：无。
- 下一步：已完成。

### 任务 6：调整员工端打卡更新流程

- 状态：已完成。
- 执行方式：AFK。
- 负责人：AI。
- 开始时间：2026-06-05 本轮实施开始。
- 完成时间：2026-06-05 21:56:44 PDT。
- 实际完成内容：
  - 员工端打卡来源改为 `mobile`，避免被计算层误判为后台人工调整。
  - 员工端当天上班打卡后立即按过程态重算，缺下班时显示 `checked_in`，不是异常。
  - 员工端打卡仍会刷新日结果和月汇总，保持后台列表可见。
- 实际输出物：
  - `apps/admin/server/index.js`
  - `supabase/migrations/20260606035000_allow_mobile_attendance_source.sql`
- 验证结果：
  - `node --check apps/admin/server/index.js` 通过。
  - `npm --workspace @wmshr/admin run lint` 通过。
- 遗留问题：目标数据库必须应用 `allow_mobile_attendance_source` 迁移后，员工端才能正式写入 `mobile` 来源。
- 下一步：已完成。

### 任务 7：调整后台人工新增 / 补卡 / 休假 / 病假流程

- 状态：已完成。
- 执行方式：AFK。
- 负责人：AI。
- 开始时间：2026-06-05 本轮实施开始。
- 完成时间：2026-06-05 21:56:44 PDT。
- 实际完成内容：
  - 后台新增 / 调整考勤继续走 `attendance_records` upsert。
  - `manual` 来源保持 `manual_adjusted`，自动底稿和结算不会绕过原始记录优先级。
  - 病假 `sick_leave` 保持独立状态，按非出勤处理，不计工时和费用。
  - 新增和调整后继续立即重算当天和当月，保证列表、月汇总、薪资输入同步。
- 实际输出物：
  - `apps/admin/server/attendance-v2.js`
  - `apps/admin/server/index.js`
  - `apps/admin/src/components/AttendanceTable.tsx`
- 验证结果：
  - `node apps/admin/server/attendance-v2.test.mjs` 通过。
  - `npm --workspace @wmshr/admin run lint` 通过。
- 遗留问题：无。
- 下一步：已完成。

### 任务 8：调整后台考勤计算列表展示

- 状态：已完成。
- 执行方式：AFK。
- 负责人：AI。
- 开始时间：2026-06-05 本轮实施开始。
- 完成时间：2026-06-05 21:56:44 PDT。
- 实际完成内容：
  - 前端类型增加 `pending`、`checked_in`。
  - 考勤表增加 `待打卡`、`已上班` 状态标签和样式。
  - `pending` / `leave` / `sick_leave` / `absent` 在列表中按非出勤状态展示工时和费用。
  - 页面底部说明改为：待打卡是当天底稿，历史缺勤是结算后无打卡记录。
- 实际输出物：
  - `apps/admin/src/types.ts`
  - `apps/admin/src/components/AttendanceTable.tsx`
  - `apps/admin/server/index.js`
- 验证结果：
  - `npm --workspace @wmshr/admin run lint` 通过。
  - `npm --workspace @wmshr/admin run build` 通过。
- 遗留问题：无。
- 下一步：已完成。

### 任务 9：建立定时任务流程

- 状态：已完成。
- 执行方式：AFK。
- 负责人：AI。
- 开始时间：2026-06-05 本轮实施开始。
- 完成时间：2026-06-05 21:56:44 PDT。
- 实际完成内容：
  - 新增 cron 入口 `GET /api/cron/attendance-nightly`。
  - 继续使用 `CRON_SECRET` 校验，不暴露给浏览器。
  - Vercel Cron 配置为 `0 17 * * *`，对应泰国时间 00:00。
  - 原薪资夜间任务调整到 `5 17 * * *`，确保考勤先结算再薪资核算。
- 实际输出物：
  - `apps/admin/server/index.js`
  - `vercel.json`
- 验证结果：
  - `node --check apps/admin/server/index.js` 通过。
  - `npm run build` 通过。
- 遗留问题：正式环境需配置 `CRON_SECRET`，并由部署平台实际触发验证。
- 下一步：已完成。

### 任务 10：建立手动补跑能力

- 状态：已完成。
- 执行方式：AFK。
- 负责人：AI。
- 开始时间：2026-06-05 本轮实施开始。
- 完成时间：2026-06-05 21:56:44 PDT。
- 实际完成内容：
  - 新增已登录后台接口：
    - `POST /api/admin/attendance-calculations/run-daily-maintenance`
    - `POST /api/admin/attendance-calculations/generate-drafts`
    - `POST /api/admin/attendance-calculations/settle-date`
  - 新增前端 API 封装，全部走当前登录账号，不暴露 `CRON_SECRET`。
  - 考勤页面增加 `生成底稿`、`结算日期`、`补跑每日维护` 按钮。
  - 补跑完成后显示成功提示并刷新当前表格。
- 实际输出物：
  - `apps/admin/server/index.js`
  - `apps/admin/src/lib/api.ts`
  - `apps/admin/src/components/AttendanceTable.tsx`
  - `apps/admin/src/types.ts`
- 验证结果：
  - `npm --workspace @wmshr/admin run lint` 通过。
  - `npm --workspace @wmshr/admin run build` 通过。
- 遗留问题：无。
- 下一步：已完成。

### 任务 11：核对月度汇总联动

- 状态：已完成。
- 执行方式：AFK。
- 负责人：AI。
- 开始时间：2026-06-05 本轮实施开始。
- 完成时间：2026-06-05 21:56:44 PDT。
- 实际完成内容：
  - 月度汇总继续由 `recalculateMonthlyAttendance` 统一刷新。
  - 月度汇总排除 `pending` / `checked_in` 过程态，避免当天底稿进入薪资前置汇总。
  - 结算服务完成后自动刷新对应月份汇总。
- 实际输出物：
  - `apps/admin/server/index.js`
- 验证结果：
  - `node --check apps/admin/server/index.js` 通过。
  - `npm --workspace @wmshr/admin run lint` 通过。
- 遗留问题：无。
- 下一步：已完成。

### 任务 12：核对薪资联动是否需要调整

- 状态：已完成。
- 执行方式：AFK。
- 负责人：AI。
- 开始时间：2026-06-05 本轮实施开始。
- 完成时间：2026-06-05 21:56:44 PDT。
- 实际完成内容：
  - 确认薪资生成入口仍会先调用 `recalculateMonthlyAttendance`。
  - 确认薪资仍以 `monthly_attendance_summaries` 为前置契约。
  - 通过任务 11 排除过程态，避免 `pending` / `checked_in` 影响当月薪资。
  - 定时任务顺序调整为考勤 00:00、薪资 00:05，避免薪资读取未结算考勤。
- 实际输出物：
  - `apps/admin/server/index.js`
  - `vercel.json`
- 验证结果：
  - `node --check apps/admin/server/index.js` 通过。
  - `npm run build` 通过。
- 遗留问题：正式数据仍需部署后用实际员工月份执行一次人工补跑或 cron 观察。
- 下一步：已完成。

### 任务 13：制定验证用例并执行验收

- 状态：已完成。
- 执行方式：AFK 执行，最终 HITL 验收。
- 负责人：AI + 用户验收。
- 开始时间：2026-06-05 本轮实施开始。
- 完成时间：2026-06-05 21:56:44 PDT。
- 实际完成内容：
  - 新增纯计算用例：
    - 无打卡 + `pendingIfNoRecord` => `pending` / `draft`。
    - 只有上班无下班 + `inProgressIfMissingOut` => `checked_in` / `draft`。
    - 只有上班无下班且正式结算 => `exception` / `OUT_TIME_MISSING`。
  - 执行服务端语法检查。
  - 执行 admin 类型检查。
  - 执行 admin 生产构建。
  - 执行根项目 lint 和 build。
- 实际输出物：
  - `apps/admin/server/attendance-v2.test.mjs`
  - 本进度文档。
- 验证结果：
  - `node apps/admin/server/attendance-v2.test.mjs` 通过。
  - `node --check apps/admin/server/index.js` 通过。
  - `node --check apps/admin/server/attendance-v2.js` 通过。
  - `npm --workspace @wmshr/admin run lint` 通过。
  - `npm --workspace @wmshr/admin run build` 通过。
  - `npm run lint` 通过。
  - `npm run build` 通过。
- 遗留问题：
  - 构建存在既有 Vite chunk size warning，不影响构建通过。
  - 本地未直接连接生产 Supabase 应用迁移；上线前需按部署流程应用迁移。
- 下一步：用户验收；如确认上线，执行数据库迁移和生产部署。

---

## 5. 本轮关键文件清单

- `supabase/migrations/20260606070000_add_attendance_draft_statuses.sql`
- `apps/admin/server/attendance-v2.js`
- `apps/admin/server/attendance-v2.test.mjs`
- `apps/admin/server/index.js`
- `apps/admin/src/types.ts`
- `apps/admin/src/lib/api.ts`
- `apps/admin/src/components/AttendanceTable.tsx`
- `vercel.json`

---

## 6. 最终验证命令记录

```bash
node apps/admin/server/attendance-v2.test.mjs
node --check apps/admin/server/index.js
node --check apps/admin/server/attendance-v2.js
npm --workspace @wmshr/admin run lint
npm --workspace @wmshr/admin run build
npm run lint
npm run build
```

以上命令均已通过。构建输出只包含 Vite chunk size warning。