# Admin 模块路由缓存 + 加载体验优化任务进度

## 当前状态
- T1 梳理一级模块路由与页面边界：已完成
- T2 建立一级模块页面路由承载结构：已完成
- T3 引入模块级路由缓存机制：已完成
- T4 改造模块数据加载策略为“保留旧数据 + 后台刷新”：已完成
- T5 统一 loading 交互语义：已完成
- T6 路由缓存与数据刷新的联动验证：已完成

## 已完成
1. 已建立确认文档：`.hermes/plans/admin-route-cache-loading-confirmation.md`
2. 已根据确认结果建立任务安排文档：`.hermes/plans/admin-route-cache-loading-task-plan.md`
3. 已确认本轮目标不是单纯换 loading 文案，而是同时调整：
   - 一级模块页面承载方式
   - 路由缓存策略
   - 数据刷新策略
   - loading 交互语义
4. 已确认当前优先范围仅限 5 个一级模块：
   - dashboard
   - employees
   - attendance
   - payroll
   - sop
5. 已核实现状：当前多个模块各自维护 loading，并存在加载时直接替换主体区域的情况。
6. 已在 `apps/admin/src/App.tsx` 建立一级模块 keep-alive 挂载层：
   - 仍沿用现有 `/:lang/:tab` 路由
   - 只为已访问模块保留页面实例
   - 模块切换时不再销毁已访问模块组件
7. 已完成 5 个一级模块的页面承载与刷新接线：
   - `Dashboard` 新增 `isActive` 激活刷新
   - `EmployeeList` 保留旧列表并展示后台刷新提示
   - `AttendanceTable` 新增 `isActive`，按当前筛选条件刷新但不清空表格
   - `PayrollTable` 新增 `isActive`，保留旧结果表并在激活后后台刷新
   - `SopManager` 新增 `isActive`，保留草稿/检索/详情状态并后台刷新 SOP 列表
8. 已按你最新要求二次调整 5 个一级模块：移除首次进入时的可见 loading 展示，首次进入直接渲染空界面与空数据；但再次刷新时保留“刷新中/后台刷新”状态提示。
9. 本轮具体落地为：
   - `Dashboard` 首次进入直接显示空 KPI/空统计，不再整页显示“正在加载数据看板”，但手动刷新时保留“正在刷新...”
   - `EmployeeList` 首次进入直接显示空列表态，不再显示“正在加载员工数据”，但后台刷新时保留提示条
   - `AttendanceTable` 首次进入直接显示空表格态，不再插入“正在加载考勤记录”占位行，但再次刷新时保留“刷新中”标记
   - `PayrollTable` 首次进入直接显示空结果态，不再显示“正在加载薪酬结果”，但再次刷新时保留“刷新中”标记
   - `SopManager` 首次进入直接显示空 SOP 列表态，不再显示“正在从后台加载SOP规范”，但后台刷新时保留提示条
10. 已完成静态验证：
   - `npm run lint --workspace @wmshr/admin` 通过
   - `npm run build --workspace @wmshr/admin` 通过

## 当前判断
1. 这次改造已经按一级模块页面架构层完成，不是只在局部组件里补 loading 特效。
2. 页面缓存和数据缓存已按两层落地：
   - 页面缓存负责保留界面实例/返回体验
   - 数据策略负责实现“先显示旧数据，再后台刷新”
3. 当前 5 个一级模块都已不再展示可见 loading；首次进入直接显示空界面，数据返回后自然回填。

## 阻塞
- 暂无

## 下一步
1. 如需补充真实交互录屏/截图验证，可在有浏览器操控条件时补跑模块切换回访用例。
2. 如后续要继续扩展到二级页、弹窗详情页、筛选滚动位保留，再在当前 keep-alive 基础上分层推进。
